import { ArbitrageTracker } from "@/components/tracker/ArbitrageTracker";

export const metadata = {
  title: "Arbitrage Tracker | Experience HK",
  description: "Track real-time flip variance for secondary market arbitrage opportunities",
};

export default function ArbitragePage() {
  return <ArbitrageTracker />;
}
